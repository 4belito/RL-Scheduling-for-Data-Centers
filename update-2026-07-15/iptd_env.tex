% ============================================================
%  update-2026-07-15/iptd_env.tex   (FRAGMENT)
%  IPTD environment (Idle + Priority + Throughput + Deadline) results.
%  \input from update-2026-07-15/main.tex; job dir = research_updates/ (root).
% ============================================================

\section{IPTD Environment}

% ---- Section divider ---------------------------------------
\begin{frame}[plain]
  \vfill
  \begin{center}
    {\Huge\bfseries\color{DeepBlue} IPTD Environment}\\[0.6em]
    {\color{SkyBlue}\rule{0.40\linewidth}{1.4pt}}\\[0.7em]
    {\large\color{DeepBlue!80!black} Idle \,$+$\, Priority \,$+$\, Throughput \,$+$\, Deadline}
  \end{center}
  \vfill
\end{frame}

% ---- Learned schedule (xsattn policy exploration) ----------
% Placed first: shows the new IPTD job distribution across the cluster.
\begin{frame}{IPTD: Learned Schedule}
  \centering
  \framedgraphic[height=0.76\textheight]{policy_exploration_IPTD}

  % \vspace{0.2em}
  % {\small\textcolor{DeepBlue}{\textbf{smdpppo\_xsattn} on the full 192-node cluster: packs the queue tightly across nodes (reward $48.6$, 360 jobs, $0.85$ utilisation).}}
\end{frame}

% ---- State space: IPTD environment -------------------------
\begin{frame}{IPTD: State Space}
  \begin{columns}[T,onlytextwidth]
    \column{0.46\textwidth}
      \begin{block}{Context (global)}
        \footnotesize
        \begin{itemize}\setlength\itemsep{2pt}
          \item \texttt{free\_frac} --- free nodes / total
        \end{itemize}
      \end{block}
      \begin{block}{Per running job \;($V_r$ soonest-ending)}
        \footnotesize
        \begin{itemize}\setlength\itemsep{2pt}
          \item \texttt{nodes}, \texttt{remaining}
        \end{itemize}
      \end{block}
    \column{0.50\textwidth}
      \begin{block}{Per queued job \;($V_q$ visible)}
        \footnotesize
        \begin{itemize}\setlength\itemsep{2pt}
          \item \texttt{nodes\_required}, \texttt{wall\_time}
          \item \texttt{priority}
          \item \texttt{deadline\_urgency} --- wall / time-to-deadline
          \item \texttt{wait\_fraction} --- queue wait / horizon
        \end{itemize}
      \end{block}
  \end{columns}

  \vspace{0.3em}
  \centering
  {\small\textcolor{DeepBlue}{Adds \textbf{priority} \& \textbf{deadline} signals to every queued job --- the policy trades off SLA urgency against packing.}}
\end{frame}

% ---- Total reward ------------------------------------------
\begin{frame}{IPTD: Total Reward}
  \centering
  \framedgraphic[height=0.80\textheight]{results_IDTP}
\end{frame}

% ---- Full scheduling metrics -------------------------------
\begin{frame}{IPTD: Scheduling Metrics}
  \centering
  \framedgraphic[height=0.80\textheight,width=0.9\textwidth]{results_IDTP_metrics}
\end{frame}

% ---- Queue length over time --------------------------------
\begin{frame}{IPTD: Queue Length Over Time}
  \centering
  \framedgraphic[height=0.80\textheight]{results_IDTP_quelength}
\end{frame}
